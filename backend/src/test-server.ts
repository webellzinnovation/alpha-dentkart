import express from 'express';
import orderRoutes from './routes/orders.js'; // Adjust based on your compilation or just run the TS file
import { errorHandler } from './middleware/errorHandler.js';

// We'll just run it via ts-node or similar.
